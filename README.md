# Roster-Scheduler

## Environment Setup

This project uses a `.env` file to set the `PYTHONPATH` so imports work correctly.

Create a `.env` file in the project root with:

```env
PYTHONPATH=src
```
